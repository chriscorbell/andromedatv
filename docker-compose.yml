services:
  andromedatv:
    container_name: andromedatv
    restart: unless-stopped
    user: "1000:1000"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3834:3001"
    env_file:
      - .env
    volumes:
      - ./data:/data
