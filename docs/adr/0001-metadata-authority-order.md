# Metadata authority order

AndromedaTV will treat Sidecar Overrides as the highest Metadata Authority, AniDB as the default external authority for Series and Episode facts, and filename or folder inference as matching hints and fallback display data. This keeps local curation explicit while avoiding silent drift when anime filenames, season numbering, specials, or AniDB ordering disagree.
