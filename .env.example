JWT_SECRET=replace_me # Replace this with a random string of at least 32 characters
ERSATZTV_BASE_URL=http://your-ersatztv-host:8409

# You can clear the chat history via API with:
# curl -X POST https://yourdomain.com/chat/admin/clear -H "Authorization: Bearer {ADMIN_USER_JWT}"